import 'package:flutter/material.dart';

class Obxxxx extends StatelessWidget {
  const Obxxxx({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text("REST APİ"),
      ),
      //     body: ,
    );
  }
}
